git init .  -> cria um repo git na pasta atual
git status -> mostra o status do trabalho (do aquivo atual )
git add nome_arquivo
git commid -m "msg" -> faz commit do que estava para ser comitado 

git log -> mostra todo o historico de commit

## fluxo de trabalho 
1. criar diretorio (pasta) do projeto 
2. iniciar o git dentro do diretório criado (git init .)
3. cria ou edita arquivos 
4. git status
5. git add <nome arquivo>
6. git status
7. git commit -m "mensagem"
8. git status
9. volta para o passo3.

